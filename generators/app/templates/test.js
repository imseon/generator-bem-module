'use strict'

var assert = require("assert");
var <%= name %> = require('./');

describe('suite', function() {
  describe('set', function() {
    it('it', function(done) {
      done();
    });
  });
});
